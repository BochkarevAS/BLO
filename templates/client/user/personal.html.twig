{% extends 'base.html.twig' %}

{% block title %}Личный кабинет{% endblock %}

{% block body %}

    <div class="container">

        <div>
            <h4>Мой профиль</h4>
        </div>

        <hr>

        <p><a href="{{ path('auth_user_index', {'id': user.id}) }}">Настройки профиля</a></p>
        <p><a href="{{ path('auth_user_show_declaration') }}">Мои объявления</a></p>
        <p><a href="{{ path('auth_user_declaration') }}">Подать объявление</a></p>
        <p><a href="{{ path('client_company_new') }}">Добавить компанию</a></p>

    </div>

{% endblock %}