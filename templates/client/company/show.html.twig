{% extends 'base.html.twig' %}

{% block title %}Company1{% endblock %}

{% block body %}
    <h1>Company1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company1.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ company1.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ company1.createdAt ? company1.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client_company1_index') }}">back to list</a>

    <a href="{{ path('client_company1_edit', {'id': company1.id}) }}">edit</a>

    {{ include('client_company1/_delete_form.html.twig') }}
{% endblock %}