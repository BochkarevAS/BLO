{% extends 'base.html.twig' %}

{% block title %}Продавцы{% endblock %}

{% block body %}

    <div class="container">

        <h4>Продавцы</h4>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ company.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ company.name }}</td>
                </tr>
                <tr>
                    <th>CreatedAt</th>
                    <td>{{ company.createdAt ? company.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('client_company_show_all') }}">Назад</a>

        {#<a href="{{ path('client_company_edit', {'id': company.id}) }}">Редактировать</a>#}

        {#{{ include('client/company/_delete_form.html.twig') }}#}

    </div>

{% endblock %}