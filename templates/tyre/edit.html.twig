{% extends 'base.html.twig' %}

{% block title %}Редактировать{% endblock %}

{% block body %}

    <div class="container">
        <h4>Редактировать </h4>

        {% for msg in app.session.flashBag.get('success') %}
            <div class="alert alert-success">
                {{ msg }}
            </div>
        {% endfor %}

        {{ include('tyre/_form_new.html.twig') }}
        {#{{ include('client/company/_delete_form.html.twig') }}#}

        {#<a href="{{ path('client_price_load', {'id': company.id}) }}">Загрузить прайс</a>#}
        {#<br>#}
        {#<a href="{{ path('client_company_index') }}">Назад</a>#}

    </div>

{% endblock %}